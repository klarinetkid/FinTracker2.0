enum Pages {
    Dashboard = "/",
    Breakdown = "/breakdown",
    Categories = "/categories",
    Formats = "/formats",
    Budget = "/budget",
    Import = "/import",
    Memos = "/memos",
    Transactions = "/transactions",
    About = "/about",
}

export default Pages;
