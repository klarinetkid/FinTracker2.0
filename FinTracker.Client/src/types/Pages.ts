enum Pages {
    Dashboard = "/",
    Breakdown = "/breakdown",
    Categories = "/categories",
    Formats = "/formats",
    Budget = "/budget",
}

export default Pages;

export type ViewEditDelete = "view" | "edit" | "new";
