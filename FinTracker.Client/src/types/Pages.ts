enum Pages {
    Dashboard = "/",
    Breakdown = "/breakdown",
    Categories = "/categories",
    Formats = "/formats",
    Budget = "/budget",
    Import = "/import",
    Memos = "/memos",
    Transactions = "/transactions",
}

export default Pages;
