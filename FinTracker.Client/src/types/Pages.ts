enum Pages {
    Dashboard = "/",
    Breakdown = "/breakdown",
    Categories = "/categories",
    Formats = "/formats",
    Budget = "/budget",
    Import = "/import",
    Memos = "/memos",
}

export default Pages;

export type ViewEditDelete = "view" | "edit" | "new";
