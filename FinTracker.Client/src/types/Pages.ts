enum Pages {
    Dashboard = "/",
    Breakdown = "/breakdown",
    Categories = "/categories",
    Formats = "/formats",
}

export default Pages;

export type ViewEditDelete = "view" | "edit" | "new";
